if ! has devenv; then
  log_error "devenv not found. Install it from https://devenv.sh"
  return 1
fi

watch_file devenv.nix
watch_file devenv.yaml
watch_file devenv.lock

eval "$(devenv print-dev-env)"
